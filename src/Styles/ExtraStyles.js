const HalfWidth = {
	width: "50%",
	/* transition: "width .15s", */
}

const FullWidth = {
	/* width: "100%", */
	transition: "width .2s",
}

const extraStyles = {
	HalfWidth: HalfWidth,
	FullWidth: FullWidth,
}

export default extraStyles
